# Monster Delivery Service

This is the Unity project used by the virtual game-development team.

- Unity version: 6000.5.10f1
- Project path: `C:\Users\user\Documents\Codex\MonsterDeliveryService`
- Read `GAME_DESIGN.md`, `TEAM_STATUS.md`, `NEXT_SESSION.txt`, and `ART_REVIEW.md` before changing the project.
- Save reviewable visual concepts as PNG, JPG, or WEBP in `art-review/`.
- Do not claim a Unity change is complete until it has been checked in the Editor or its limitation is recorded.
- Keep the current task small, testable, and documented in `NEXT_SESSION.txt` before handoff.
- Ponytail rule: prefer the smallest correct implementation; reuse existing code and Unity features; do not add a dependency, abstraction, or speculative option without a current need; leave one focused verification for non-trivial logic.
- Serena rule: activate this directory as the Serena project at the start of a session, then prefer Serena for semantic code exploration and targeted edits when its tools are available.
- Git manager rule: review `git status` and diff before handoff; summarize changes in Korean. Create commits only when the user explicitly approves, and never push, create PRs, reset, checkout, rebase, or delete Git data without approval.
