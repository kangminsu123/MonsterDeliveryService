# UI 아트 수정 요구사항 — 기괴한 병맛 지원 시안 v1

## 목적과 범위

`art-review/mvp-start-result-ui-support-concept-v1.png`의 검수 피드백(“더 병맛에 기괴하게, 너무 귀엽지 않게”)을 반영한다. 이미 동작하는 단일 플레이 배송 루프와 시작/결과 화면의 정보 구조는 바꾸지 않는다. 이번 작업은 검수용 아트 시안과, 승인되었을 때 적용 가능한 최소 UI 표현 변경만 대상으로 한다.

## 유지할 플레이/정보 구조

- 시작 화면: 주문 시작 버튼과 13번지 배송의 목표를 즉시 알 수 있다.
- 결과 화면: 성공/실패, 소포 상태, 경과 시간, 별점을 계속 표시한다.
- 기존 버튼의 의미, 입력 흐름, 결과 판정(`완벽` 3성, `손상` 2성, `폐기물`/시간 초과 0성)은 변경하지 않는다.

## 아트 방향

- 귀여운 둥근 스티커/마스코트 인상을 줄이고, 비대칭·삐뚤어진 실루엣과 불길한 배송 사고의 인상을 준다.
- 시작 패널은 찌그러진 금속 표지판 또는 낡은 우편함처럼 보이게 한다. 한쪽 눈알, 이빨 자국, 침 자국, 비정상적으로 긴 손가락 같은 요소는 장식으로만 사용한다.
- 결과 패널은 망가진 배송 영수증/경고 계기판처럼 보이게 한다. 성공은 불편할 정도로 과장된 도장, 실패는 고장 난 경고등 또는 얼룩으로 표현한다.
- 텍스트가 있는 버튼·상태·시간·별점 영역은 장식과 분리해 높은 대비를 유지한다. 신체 장식은 텍스트와 버튼의 클릭 영역을 가리지 않는다.
- 기본 야간 팔레트(짙은 남색·보라, 청록 정보 영역, 주황 강조)는 유지하되, 탁한 점액 녹색·멍든 보라·과포화 적색을 소량의 오염/경고 악센트로 쓴다.
- 공포 사실성, 혈흔, 과도한 신체 훼손, 작은 글자의 난독화는 제외한다. 목표는 ‘기괴하고 우스운 배송 사고’이지 잔혹물이 아니다.

## 최소 구현 요구사항

1. 승인 전에는 게임 코드/프리팹/UI 구조를 수정하지 않고 검수용 PNG·JPG·WEBP 시안만 `art-review/`에 추가한다.
2. 승인 후 적용 시 기존 `MonsterDeliveryPrototype`의 텍스트, 버튼 이벤트, 결과 상태 계산을 재사용한다. 새 패키지·폰트·셰이더·애니메이션 시스템은 추가하지 않는다.
3. 구현 변경은 기존 Unity IMGUI 스타일 값(색상, `GUI.Box`/`GUI.Label`/`GUI.Button` 주변 장식) 안에서 끝낸다. 기능 제어와 장식 렌더링을 분리하기 위한 새 추상화는 만들지 않는다.
4. 장식은 시작 및 결과 화면에만 한정한다. 게임플레이 HUD와 월드 오브젝트, 충돌/배송/차단기 로직은 그대로 둔다.

## 검증 가능한 수용 기준

1. `art-review/`에 수정 방향을 확인할 수 있는 PNG, JPG, WEBP가 각각 존재하며, 시작 화면과 결과 화면의 기괴한 병맛 요소가 보인다.
2. 시안에서 시작 버튼, 성공/실패, 소포 상태, 경과 시간, 별점 텍스트를 일반 화면 크기에서 읽을 수 있다.
3. 시안은 둥근 귀여운 캐릭터 중심 표현 대신 최소 두 가지 이상의 비대칭/불길한 배송 장식(예: 찌그러진 표지판, 눈알, 이빨 자국, 침 자국, 고장 경고등)을 사용한다.
4. 실제 적용 범위는 시작/결과 UI의 표현뿐이며, 주문 시작·소포 운반·차단기 A/B·문·집 정지·우편함·별점 판정의 동작이 변경되지 않는다.
5. Unity Editor Play Mode에서 시작 버튼을 눌러 배송을 시작하고, 결과 화면에서 성공/실패·소포 상태·고정된 경과 시간·별점이 여전히 표시되는지 한 번 확인한다.

## 집중 검증

결과 화면 장식이 `소포 상태 / 경과 시간 / 별점`의 텍스트와 겹치지 않아, 완료 판정 후 네 가지 결과 정보(성공/실패 포함)를 한 화면에서 읽을 수 있는지 확인한다.
